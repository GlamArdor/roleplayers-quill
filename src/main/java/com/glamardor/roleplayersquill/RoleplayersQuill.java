package com.glamardor.roleplayersquill;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

/** Names the mod once, so nothing else has to spell it. */
public final class RoleplayersQuill {
	public static final String MOD_ID = "roleplayersquill";
	public static final Logger LOGGER = LoggerFactory.getLogger("Roleplayer's Quill");

	private RoleplayersQuill() {
	}
}
